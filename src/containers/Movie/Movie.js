import React from 'react';
import MovieCard from '../../components/MovieCard/MovieCard';

function Movie() {
  return (
    <div className="Movie">
      <MovieCard/>
      
    </div>
  );
}

export default Movie;